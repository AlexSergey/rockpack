# Plan 2: Implement the audit recommendations

Status: done on 2026-09-24 (A6 and the starter-e2e part of I1 moved to Plan 4)
Order: second, after Plan 1 (the unit tests are the safety net for the behavioural changes below)
Owner: TBD

## 1. Goal

Close every finding from the 2026-09-23 audit of the repository: infrastructure (CI, hooks, publishing, root scripts), dependency hygiene, Node/engine alignment, known bugs, error handling, lint rules, duplicated code, and the test-suite issues in `e2e/*`, `examples/*` and the starter templates.

Each item states the files involved, the change, and the acceptance check. Items are grouped by area; the order inside each group is the suggested execution order. Every user-facing change gets a CHANGELOG entry under `9.0.0`.

## 2. Ground rules

- Behavioural changes to a package are made only when that package's unit tests from Plan 1 exist and pass before and after the change. Update or add the tests in the same commit.
- One area per pull request; one commit per numbered item where practical.
- No new abstractions for one-off fixes. Reuse `@rockpack/utils` for anything shared by two or more packages.
- Prefer `type` over `interface`, no `any`, minimal diffs, no unrelated reformatting.

## 3. Area A: CI and git hooks

- [x] **A1. CI workflow.** Add `.github/workflows/ci.yml` triggered on `push` to `next` and `master` and on `pull_request`. _Done 2026-09-24: `.github/workflows/ci.yml` (check, e2e-light, examples-build, nightly/dispatch starter-e2e); all jobs pass locally; `e2e/compiler-e2e` joins e2e-light when Plan 4 adds it._ _Plan 4 step 8 (2026-09-24) replaced `e2e-light`, `examples-build` and `starter-e2e` with `e2e-pinned` and `e2e-runtime`, and added `e2e-latest.yml` and `deps-canary.yml`; nightly schedules are off for now._
  Jobs:
  1. `check` (ubuntu-latest, `actions/setup-node` with `node-version-file: .nvmrc`, `cache: npm`): `npm ci`, `npm run build`, `npm run lint`, `npm run test:unit`, upload `packages/*/coverage/lcov.info` as artifacts.
  2. `e2e-light` (needs `check`): `npm test -w e2e/babel-e2e`, `npm test -w e2e/compiler-e2e`, `lerna run test --scope='@rockpack/example-tester-*'`.
  3. `examples-build` (needs `check`): `lerna run build --scope='@rockpack/example-compiler-*'` (compiler examples currently have no automated check at all).
  4. `starter-e2e` (needs `check`, `workflow_dispatch` and nightly `schedule` only until item A6 makes it deterministic): installs Chromium through `npx puppeteer browsers install chrome`, runs `npm test -w e2e/starter-e2e`.
  Acceptance: green run on `next`; a PR that breaks lint or a threshold fails.
- [x] **A2. Pages workflow.** `.github/workflows/pages.yml` currently deploys the committed `docs/` build on push to `master`. Keep the trigger but add `paths: ['docs/**']` so plan documents and unrelated commits do not redeploy. Building `book` in CI is deferred to Plan 3 because `book/scripts.build.ts` prerenders with a local `/Applications/Chromium.app` in non-headless mode. _Done 2026-09-24._
- [x] **A3. Install hooks on clone.** Root `package.json`: add `"prepare": "simple-git-hooks"`. Acceptance: fresh clone plus `npm i` produces `.git/hooks/pre-commit`. _Done 2026-09-24._
- [x] **A4. Lighter hooks.** Add `lint-staged` (devDependency, root) with `"*.{ts,tsx,js,mjs,cjs}": "eslint --fix"` and `"package.json": "eslint --fix"`. `simple-git-hooks.pre-commit` becomes `npx lint-staged`; `pre-push` becomes `npm run lint && npm run test:unit`. The full `npm test` (e2e and examples) runs in CI only. Acceptance: committing a single file takes seconds, not minutes. _Done 2026-09-24: root `lint-staged.config.mjs` groups staged files by workspace and runs `npm exec -w <workspace> -- eslint --fix` there (the codestyle config resolves tsconfig and package.json from cwd); a commit now takes seconds._
- [x] **A5. Dependency update automation.** Add `renovate.json` (or `.github/dependabot.yml`) with grouped weekly PRs for `@babel/*`, `@typescript-eslint/*`, `eslint*`, `jest*`, `webpack*`, and `rangeStrategy: pin`. The in-repo `updater.ts` stays for bulk local runs. Acceptance: first bot PR opened and CI runs on it. _Done 2026-09-24: `.github/dependabot.yml` (npm grouped weekly into `next`, `versioning-strategy: increase` keeps exact pins, plus github-actions)._
- [x] **A6. Make `e2e/starter-e2e` deterministic.** The full rewrite is specified in [Plan 4, section 4](./04-e2e.md); the list below is the minimum set of fixes and stays here for traceability. Note that Plan 4 keeps `--mode=test` resolving current versions (its `latest` mode depends on it); the "no network" requirement applies to Plan 4 `pinned` mode only. _Moved to Plan 4 section 4 on 2026-09-24: the suite is rewritten there, and checking generated projects needs the pinned mode (npm pack tarballs) because `--mode=test` installs the published 8.0.0 packages, whose tester and codestyle APIs differ from the current sources (seen while verifying E2 and E7). The describe-structure part of I1 for `e2e/starter-e2e` moves with it._ _Done by Plan 4 steps 2 to 4 (2026-09-24): `generators.spec.ts`, `simple-flow.spec.ts`, `utils/*` and the strict screenshots are gone; the suite is `cli`, `generation`, `quality` and `runtime`._
  Original list (`e2e/starter-e2e/src/generators.spec.ts`, `simple-flow.spec.ts`, `utils/*`):
  - replace the three hardcoded `executablePath: '/opt/homebrew/bin/chromium'` with `process.env.PUPPETEER_EXECUTABLE_PATH ?? undefined` so puppeteer's bundled browser is used by default;
  - clear `screenshots/new/` in `beforeAll`, rethrow screenshot errors instead of `console.error`, assert the file exists before `looksSame`;
  - replace `{ strict: true }` pixel comparison with a tolerance (`tolerance: 5, ignoreAntialiasing: true`) or, better, assert DOM content through page selectors and keep a single smoke screenshot;
  - kill the whole process group (`spawn` with `detached: true` then `process.kill(-pid)`), and pick free ports through `find-free-port` instead of fixed 3000/4000/35729;
  - fix the `tsx ./src/csr.server.js` spawn (the file is `.ts`) and pass an explicit `cwd`;
  - move project generation into `beforeAll` so individual `it` blocks can run alone; replace the repeated ANSI-strip/`execSync`/`exists` blocks with one helper in `src/utils/run.ts`;
  - fix the `generates project with type %i` title (use `it.each` with `%s`), remove `any` casts in catches;
  - `simple-flow.spec.ts` "no name" test must use `expect(() => execSync(...)).toThrow(/Please specify/)`;
  - `--mode=test` must not hit the network: see B6 (starter resolves versions offline in test mode).
  Acceptance: the suite passes twice in a row on macOS and in the CI job from A1 without network after install.

## 4. Area B: root scripts, publishing, versions

- [x] **B1. Declare root script dependencies.** Root `package.json`: add `tsx`, `semver`, `latest-version`, `yargs`, `@types/semver`, `@types/yargs` to `devDependencies` (versions aligned with the packages); move `sort-package-json` to `devDependencies`; remove `@inquirer/select` (unused at root). Acceptance: `npm run updater -- --dry-run`, `npm run version:set -- 9.0.0-next.0` and `npm run cloc` run on a clean install. _Done 2026-09-24._
- [x] **B2. Derive workspace lists from `workspaces`.** Add `tools/workspaces.ts` exporting `getWorkspacePackageJsons(): string[]` (uses `fs.globSync` over the root `workspaces` globs, Node 22+). Use it in `updater.ts` and `version.ts` and delete both hardcoded lists. This removes the `examples/compiler/source-js` phantom path in `version.ts` and includes `book`, `packages/tsconfig` and the root in the updater. Acceptance: both scripts touch exactly the set of `package.json` files that `npm query .workspace` reports. _Done 2026-09-24: `tools/workspaces.ts`; the set matches `npm query .workspace`._
- [x] **B3. Make `version.ts` atomic.** Read and validate every file first, then write all of them; a failure before the write phase leaves the tree untouched. Also validate the version with `semver.valid` instead of the manual split. Acceptance: pointing the script at a missing file changes nothing. _Done 2026-09-24._
- [x] **B4. `updater.ts` options.** Add `--dry-run` (print the plan, write nothing), `--no-major` (skip major bumps and list them at the end), and a concurrency limit of 8 for the registry calls (`latest-version` and the peer metadata fetch). Keep the peer-conflict resolution added on 2026-09-23. Acceptance: `--dry-run` output lists the skipped majors; runtime under one minute. _Done 2026-09-24: `npm run updater -- --dry-run --no-major` takes about 20 s._
- [x] **B5. Resolve the versioning model.** `lerna.json` says `"version": "independent"` while `version.ts` forces one version on every workspace. Decide for fixed mode: set `"version": "8.0.0"` in `lerna.json` (fixed mode) and keep `version:set` as the single tool, or drop `version.ts` in favour of `lerna version --no-push --no-git-tag-version`. Recommended: fixed mode plus `version.ts` (already handles the `@rockpack/*` cross-references). Acceptance: `lerna.json` and the script agree, documented in README "Release". _Done 2026-09-24: fixed mode; `version:set` also writes `lerna.json`; the examples moved from 1.0.0 to the shared version; README "Release"._
- [x] **B6. Publishing safety.** Every publishable package (`babel`, `codestyle`, `compiler`, `starter`, `tester`, `tsconfig`, `utils`): add `"prepublishOnly": "npm run build"` (tsconfig: none needed), add `lint` to the `production` script of `starter` and `tsconfig`, and remove `index.webview.ejs` from `packages/compiler/package.json` `files` (the file does not exist). Root `production` stays `lerna run production`. Acceptance: `npm pack --dry-run -w packages/<name>` lists only `lib`, `types`, README and LICENSE. _Done 2026-09-24: also added `banner` to the compiler `files` (the published compiler could not find it, so banners were always off) and a LICENSE to every publishable package. `tsconfig` has no lint script, so its `production` is unchanged._

## 5. Area C: dependency hygiene

Every removal below was found by grepping `src` for imports. Before removing, confirm again with `npx knip` (C6) and a full `npm run build && npm run lint && npm run test:unit`.

- [x] **C1. `@rockpack/compiler`.** Remove `colors`, `async`, `cross-env`, `moment` (replace the duration format in `src/utils/log.ts` with plain arithmetic that keeps hours), `@rockpack/codestyle` (pulls all of ESLint and Stylelint into every consumer). Verify and then remove the unreferenced loaders `arraybuffer-loader`, `imports-loader`, `null-loader`, `script-loader`, `svg-inline-loader`. Keep `eslint` as devDependency only. _Done 2026-09-24: `moment` replaced by arithmetic that keeps hours in the minutes; `@rockpack/codestyle` was already a devDependency only. Along the way: the elm example failed to compile while the build exited 0; `core/run.ts` now exits 1 when production stats have errors, and the example keeps the default resolve extensions._
- [x] **C2. `@rockpack/babel`.** Remove `@babel/generator`, `@babel/parser`, `@babel/traverse`, `update-browserslist-db`, `valid-types`, `@rockpack/tsconfig`; move `babel-plugin-add-import-extension` to `devDependencies` (build-only); make `react-compiler-runtime` a `peerDependency` (optional) instead of a dependency, documented in the babel README. _Done 2026-09-24: `@rockpack/tsconfig` moved to devDependencies (tsconfig `extends` needs it); `react-compiler-runtime` is an optional peer `^1.0.0`._
- [x] **C3. `@rockpack/tester`.** Remove `@adobe/css-tools`, `isomorphic-fetch`, `pretty-format`, `source-map`, `valid-types`, `yargs`, `yargs-unparser`. Keep `@types/jest` in `dependencies` on purpose (consumers write specs against jest globals) and add a comment in the README explaining why. _Done 2026-09-24._
- [x] **C4. `@rockpack/codestyle`.** Remove `@rockpack/babel`, `yargs`, `deep-extend`. Decide for each of `eslint-plugin-json`, `stylelint*`, `@commitlint/*`: either export a real config for it from `src` (see Plan 3, codestyle section) or remove it and the README claim. Until then, they stay. _Done 2026-09-24 (`@rockpack/babel` was already gone); json/stylelint/commitlint stay until Plan 3._
- [x] **C5. `@rockpack/utils` and `@rockpack/starter`.** utils: remove `valid-types`. starter: remove `ncp` (replaced by `src/utils/copy.ts`); pick one prompt library (`@inquirer/select` plus `@inquirer/confirm`, dropping `inquirer`), covered by the wizard tests. _Done 2026-09-24: `@inquirer/confirm@6.1.1` shares `@inquirer/core` 11 with `@inquirer/select@5.2.0`._
- [x] **C6. Automate.** Add `knip` at the root with a `knip.json` covering `packages/*` and `e2e/*`, and run `npx knip` in the CI `check` job. Add `syncpack` (`syncpack lint` in CI) to keep the same dependency at the same version across workspaces; fix the current mismatches: `@inquirer/select` (root 5.0.4 vs starter 5.2.0), `sort-package-json` (3.6.0 vs 3.6.1), `cross-env` (7.0.3 vs 10.1.0). _Done 2026-09-24: `knip.json`, `.syncpackrc.json` (peer ranges and book puppeteer 7 ignored with reasons), root `npm run lint:deps`, CI `check` runs it. Versions aligned: react/react-dom/@types/react* 19.3.0, @unhead/react 3.4.1, tsx 4.23.15. knip also surfaced real undeclared deps (`@types/babel__core` for the published babel types, `@rockpack/tsconfig` in four packages, `jest-environment-jsdom` in compiler, `@types/jest` in e2e) and unused exports, all fixed._
- [x] **C7. Hoist shared dev tooling.** Move `tsx`, `typescript`, `@types/node`, `cross-env` to root `devDependencies` and delete them from the workspaces that only use them for scripts (keep `typescript` as a dependency where it is imported at runtime: compiler, tester). Acceptance: `syncpack lint` clean, `npm ls typescript` shows one version. _Done 2026-09-24 together with D2: root has `tsx`, `typescript`, `@types/node`, `cross-env`, `eslint`; removed from packages, e2e and book. Examples keep their own `tsx`/`@types/node` on purpose: they document what a user installs._

## 6. Area D: Node and engines

- [x] **D1. One Node baseline.** Set `engines.node` to `>=24` in root and every package (Node 23 is end-of-life; `@babel/core@8`, which the updater will propose, requires `^22.18 || >=24.11`). Update `packages/starter/src/bin/index.ts` `minVer` to 24, README "Requirements", and the starter README. Add `engine-strict=true` to `.npmrc`. _Done 2026-09-24._
- [x] **D2. One `@types/node`.** Pin `@types/node@24.x` at the root only (C7) and remove the `26.x` copies from babel, compiler and utils. _Done 2026-09-24: `@types/node@24.13.6` at the root; examples aligned to it._
- [x] **D3. `@rockpack/tsconfig` variants.** Add `tsconfig.node.json` (no `dom` lib, `types: ["node"]`) and switch utils, starter, tester and compiler core to it; remove the stray `outDir: ./dist` and `strictPropertyInitialization: false` from the base config after confirming no package relies on it. Export both files from the package (`exports` map). _Done 2026-09-24: utils, starter and tester extend `@rockpack/tsconfig/tsconfig.node.json`; the compiler stays on the base config because `plugins/reloader/ssr.ts` runs in the browser. `outDir` and `strictPropertyInitialization: false` removed; all 39 workspaces lint clean._

## 7. Area E: known bugs

- [x] **E1. Starter update check** (`packages/starter/src/bin/rockpack.ts`): compare with `semver.gt(latest, current)`; fix the inverted message ("A newer Rockpack version is available"); wrap `latestVersion` in `try/catch` so the CLI works offline; skip the check entirely in `--mode=test`. _Done 2026-09-24: `semver.gt`, message "A newer Rockpack version is available!", offline-safe, skipped with `--mode=test`._
- [x] **E2. Starter git hooks** (`packages/starter/src/utils/git-hooks.ts`, roadmap item "Fix husky initialization"): `npm set-script` (removed in npm 9) and `husky add` (removed in husky 9) no longer exist. Replace with writing `.husky/pre-commit`, `.husky/pre-push`, `.husky/commit-msg` files directly plus `"prepare": "husky"` in the generated `package.json`, or switch generated projects to `simple-git-hooks` (smaller, no shell files). Recommended: `simple-git-hooks`, mirroring the monorepo. Update `versions.json` `git.common` accordingly and the starter-e2e assertions. _Done 2026-09-24: generated projects get `simple-git-hooks` (`versions.json` git group), a `prepare` script and a `simple-git-hooks` field (pre-commit, commit-msg, pre-push with tests); `gitHooks` runs `npx|yarn simple-git-hooks`. Verified on a real generated csr project: hooks are installed, pre-commit runs lint-staged, commit-msg rejects a non-conventional message. The generated project still fails its own lint with the published `@rockpack/codestyle@8.0.0` (no `tsconfig.eslint.json` support there) and its tests with the published tester 8.0.0 (older API); both pass with the local packages. `--mode=test` cannot validate unreleased packages, which is what Plan 4 `pinned` mode (npm pack tarballs) fixes._
- [x] **E3. Starter `.` handling** (`rockpack.ts`): `return process.exit(1)` and the `readdirSync` on a non-existent path; guard with `existsSync`. _Done 2026-09-24: the `existsSync` guard was already there; the exit is explicit now and a test pins that `readdirSync` is not called for a missing directory._
- [x] **E4. Compiler `mergeConfWithDefault`** (`src/utils/merge-conf-with-default.ts`): in the "dist is a folder" branch `distContext` is set to the file path instead of its directory; align with the other branches. _Done 2026-09-24._
- [x] **E5. Compiler `makeBanner`** (`src/modules/make-banner.ts`): `indexOf(type) > 0` misses a placeholder at index 0; use `includes`. Resolve the banner file relative to the package root through `import.meta.url` instead of `../../..`. _Done 2026-09-24: placeholders are matched as `${type}`; the banner is found from the nearest package.json (works from `src` and `lib`)._
- [x] **E6. Compiler small fixes:** stray `"` in the `SsrDevelopment` plugin name (`plugins/ssr-development/index.ts`); suspicious regex `/\.wasm(\.js)$/` in `modules/make-modules.ts` (should be `/\.wasm$/` or `/\.wasm\.js$/`, decide by the intended loader); `generate-dts.ts` `require.resolve('../constants.mjs')` fails when executed from `src` (use a static import); `sourceCompiler` swallows errors from `sourceCompile` and `generateDts` (log and rethrow so builds fail). _Done 2026-09-24: plugin name fixed; wasm rule is `/\.wasm$/`; `generate-dts` uses `mkdtemp` in the OS temp dir (no more `require.resolve('../constants.*')` or writes into the package); `sourceCompiler` logs and rethrows, and `run` exits 1 when compiling library sources fails. `utils/generate-string.ts` became dead and was removed._
- [x] **E7. Starter SSR template hook** (`packages/starter/templates/backbone/ssr/src/hooks/rockpack-description.hooks.ts`): the `catch` branch never calls `setLoading(false)` (the csr version does). Add the negative test in both `hooks/*.spec.tsx` templates. _Done 2026-09-24: negative case added to both templates; verified in generated csr and ssr projects (the ssr case fails without the fix)._
- [x] **E8. Tester examples `test:watch`**: only `examples/tester/debug/scripts.tests.ts` reads the `watch` argument. Either make `@rockpack/tester` read `--watch` from `process.argv` (recommended, documented in the tester README) or fix the nine scripts. The starter templates use `--watch`, so the tester-side fix makes both conventions work. _Done 2026-09-24: tester reads `--watch` unless `watch` is passed; the ten examples use `--watch`; the debug example lost its own parsing._
- [x] **E9. Tester README** documents `const tests = require('@rockpack/tester'); tests();` but there is no default export. Fix the README (`import { tester } from '@rockpack/tester'`). _Done 2026-09-24._
- [x] **E10. `packages/tester/tsconfig.types.json`** includes a non-existent `src/globals.d.ts`; remove it. `packages/compiler/scripts/build.ts` filters `index.cts` which no longer exists; remove the filter. _Done 2026-09-24._
- [x] **E11. `import.meta.url` in tests breaks the `__filename` idiom.** The `isTest` preset of `@rockpack/babel` uses `babel-plugin-transform-import-meta`, which rewrites `import.meta.url` to `require('url').pathToFileURL(__filename)`. A module that declares `const __filename = fileURLToPath(import.meta.url)` (the common ESM idiom) then reads its own binding in the TDZ and fails to load under `@rockpack/tester` (`Cannot access '_filename' before initialization`). Found in Plan 1 Phase 5; `packages/starter/src/lib/package-json-preparing.ts` was renamed to `currentDir` as a local fix. General fix: a small path-resolved Babel plugin (the tester serializes its config to JSON, so function plugins are dropped) that renames module-level `__filename`/`__dirname` bindings before `transform-import-meta` runs, with a spec in `packages/babel` and a tester example. Acceptance: a spec file importing a module with that idiom passes under `@rockpack/tester`. _Done 2026-09-24: `@rockpack/babel/plugins/rename-cjs-globals` (plain CJS, exported and published) runs before `transform-import-meta` in the `isTest` preset; babel spec executes the transformed idiom; `e2e/babel-e2e/src/current-file.spec.ts` proves it under `@rockpack/tester`._

## 8. Area F: error handling in `@rockpack/compiler`

Do this after the Plan 1 compiler tests exist; each step keeps the test-suite green.

- [x] **F1. Typed errors.** Add `src/errors/rockpack-error.ts` with `class RockpackError extends Error { readonly code: 'INVALID_CONFIG' | 'INVALID_ENTRY' | 'BUILD_FAILED' | 'DTS_FAILED' }`. Replace the string constants in `src/errors/*.ts` with factory functions returning `RockpackError`. _Done 2026-09-24: `errors/rockpack-error.ts`; `errors/isomorphic-compiler.ts` and `errors/library-compiler.ts` export factories._
- [x] **F2. Throw, do not exit, in library code.** Remove `process.exit` from `compilers/isomorphic-compiler.ts`, `compilers/library-compiler.ts`, `modules/make-entry.ts`, `utils/find-html.ts`, `utils/common-multi-validators.ts` and `core/run.ts`; throw `RockpackError` or reject instead. Delete the dead `find-html.ts`, `common-multi-validators.ts` and `errors/markup-compiler.ts` (unused). _Done 2026-09-24: validation throws; `find-html.ts`, `common-multi-validators.ts`, `errors/markup-compiler.ts` deleted (H6). `isomorphicCompiler` closes the live reload server when validation fails._
- [x] **F3. Exit only at the boundary.** The exported compiler functions (`frontendCompiler`, `backendCompiler`, `libraryCompiler`, `isomorphicCompiler`, `sourceCompiler`) catch `RockpackError`, log it through `utils/log.ts`, set `process.exitCode = 1`, and reject. Production builds still terminate the process at the end of `run` (webpack keeps handles open), but with `process.exitCode` set from the stats instead of `exit(0)` on error. _Done 2026-09-24: `compilers/error-boundary.ts` wraps every exported compiler (logs through `utils/log.ts#logError`, sets `process.exitCode = 1`, rejects); `sourceCompiler` maps failures to `BUILD_FAILED`/`DTS_FAILED`. Production `run` sets `process.exitCode` from the stats and calls `compiler.close()`; verified on all 20 compiler examples that the process ends on its own, which required starting live reload only in development in `isomorphicCompiler` (the server kept production builds alive). `process.exit` now only appears in `error-handler.ts`._
- [x] **F4. `error-handler.ts`.** Register the process listeners once (module-level guard), set `process.exitCode = 1` and rethrow in `uncaughtException`/`unhandledRejection`, exit with 130/143 on SIGINT/SIGTERM, delete the empty `try {}`. _Done 2026-09-24: registered once; a `RockpackError` rejection only marks the exit code (the boundary logged it), other unhandled rejections and uncaught exceptions log and exit 1 (the old handler swallowed them); SIGINT 130, SIGTERM 143; the duplicate SIGINT exit in `SsrDevelopment` is gone._
- [x] **F5. Document** the new behaviour in the compiler README (scripts can `await frontendCompiler(...)` and catch `RockpackError`). _Done 2026-09-24: README "Errors and exit codes"; `RockpackError` and `RockpackErrorCode` are exported from `@rockpack/compiler`._

## 9. Area G: lint rules and type style

- [x] **G1. `type` over `interface`** (`packages/codestyle/src/index.ts`): add `'@typescript-eslint/consistent-type-definitions': ['error', 'type']`, drop the interface naming-convention entry, remove the removed `@typescript-eslint/ban-types` and deprecated `no-empty-interface` rules. Run `npm run format` across the repo (about 45 interfaces convert) and review the diff for `declare module` blocks that must stay as interfaces (module augmentation). _Done 2026-09-24: `npm run format` converted the repository; the starter templates (excluded from the monorepo lint) were converted by hand and checked in generated csr/ssr projects. The only remaining interface is the `Window` augmentation in the ssr template `client.tsx`, with a documented disable comment. CLAUDE.md and the lint rules now agree._
- [x] **G2. Test-file override** in codestyle (done partially in Plan 1, Phase 0.4): jest globals for `*.spec.{ts,tsx}`, `eslint-plugin-jest` recommended rules (`no-disabled-tests`, `no-focused-tests`, `valid-expect`, `prefer-to-have-length`), keep `no-only-tests`. _Done 2026-09-24: `eslint-plugin-jest` in codestyle, rules scoped to specs and fixtures; one violation fixed in the sinon example._
- [x] **G3. Import extension convention.** Pick `.js` for relative imports inside `packages/*/src` (NodeNext-compatible, already used by compiler and tester) and enforce it with `import-lite/extensions` or `@typescript-eslint` `consistent-type-imports` plus a `no-restricted-syntax` pattern for `.ts` extensions. Fix `packages/starter/src/lib/install.ts` (`.ts` imports) and the extension-less imports across starter. Also decide on `allowImportingTsExtensions` in the shared tsconfig (it currently allows the inconsistency). _Done 2026-09-24: 164 specifiers in 42 files rewritten by a resolving script (files or `index` folders); each package `eslint.config.ts` adds a `no-restricted-syntax` rule for extension-less relative imports and each package tsconfig sets `allowImportingTsExtensions: false`. The shared codestyle is unchanged on purpose: user projects and templates use extension-less imports._
- [x] **G4. Unsafe casts.** `compiler/src/modules/make-plugins.ts` (`page as any`, `patterns: _prop as any`, the `Dotenv` private-field delete through `as unknown as`), `make-optimization.ts` (`as any`), `core/compile.ts` and `plugins/ssr-development/index.ts` (`as unknown as`), `codestyle/src/index.ts` (`tseslintPlugin as unknown as ESLint.Plugin`). Type `HtmlPage` and `CopySpec` against `HtmlWebpackPlugin.Options` and `CopyWebpackPlugin`'s pattern type; drop the Dotenv hack by passing `systemvars: false`/`expand` options instead of deleting a private field, or by upgrading `dotenv-webpack`. Acceptance: `grep -rn "as any\|as unknown as" packages/*/src` returns nothing except documented exceptions. _Done 2026-09-24: no `as any` / `as unknown as` left in package sources. The dotenv "hack" turned out to be dead code (dotenv-webpack 9 computes definitions inside `apply`), so it was removed without switching to `ignoreStub`, which would change runtime behaviour; nodemon options use `NodemonSettings` (`watch` is an array); html pages and copy patterns use the plugins' own option types; codestyle uses `tseslint.plugin`; `compile.ts` keeps one plain `as` for the webpack function like `isomorphic-compiler.ts`._

## 10. Area H: duplicated and dead code

- [x] **H1. `readPackageJson(dir)`** in `@rockpack/utils` (returns `PackageJson | undefined`, swallows ENOENT and JSON errors). Use it in `babel/src/index.ts`, `codestyle/src/index.ts`, `compiler/src/core/make.ts`; delete the three local `PackageJson` interfaces in favour of one exported `type PackageJson` from utils. _Done 2026-09-24: `readPackageJson`/`PackageJson` in utils; babel and codestyle now depend on utils (root `build` builds utils first). Because that made babel and codestyle import utils, utils dropped `yargs` (ESM-only, it broke Jest consumers such as `e2e/babel-e2e`): `getMode` reads `--mode=<v>`/`--mode <v>` from `process.argv` itself._
- [x] **H2. `getMode`/`setMode`.** Implement `setMode` as `getMode` plus the two env writes; parse argv lazily inside the function (this also removes the import-time side effect that Plan 1 works around). Delete `compiler/src/utils/other.ts#getMajorVersion` (utils has the canonical one). _Done 2026-09-24: `setMode` = `getMode` + env writes, no import-time parsing (with H1 no yargs at all); the compiler `getMajorVersion` copy is gone._
- [x] **H3. Shared build script.** Create a private workspace `tools/build` (not published) exporting `buildPackage({ formats: ['esm','cjs'], copyDirs, importMeta })` used by `packages/{babel,utils,tester,compiler}/scripts/build*.ts` and a single `clean` implementation. The four `scripts/clean.ts` copies and the two near-identical build scripts per package collapse to one-line callers. Keep `sourceCompiler` for codestyle and starter (they already use it). The compiler cannot build itself with `sourceCompiler`, which is why the tool lives outside the packages. _Done 2026-09-24 as the private workspace `tools/build-tools` (`@rockpack/build-tools`; `tools/build` is matched by the `build` rule in `.gitignore`). `buildPackage({ root, formats, importMeta, copyDirs })` and `cleanPackage(root)`; babel, codestyle, compiler, tester and utils use it (codestyle had copies of the scripts too), starter uses `cleanPackage`. The emitted `lib/` is byte-identical to the previous scripts. Found along the way: the compiler resolved `babel-plugin-add-import-extension` and `@babel/plugin-transform-modules-commonjs` at runtime while declaring them as devDependencies; they are dependencies now._
- [x] **H4. Complexity hot-spots.** Split `compiler/src/modules/make-plugins.ts` (`getPlugins`, about 220 LOC, sonar disabled) into `plugins/html.ts`, `plugins/env.ts`, `plugins/banner.ts`, `plugins/copy.ts`, `plugins/development.ts`, `plugins/production.ts`, `plugins/analyzer.ts`, each returning `Collection` entries; the same for `codestyle/src/index.ts` (316 LOC, sonar disabled) into `rules/typescript.ts`, `rules/react.ts`, `rules/style.ts`, `rules/files.ts`, `rules/tests.ts`. Remove the `@sonar/cognitive-complexity` disables afterwards. Unit tests from Plan 1 cover both before the split. _Done 2026-09-24: `compiler/src/modules/plugins/{checks,env,banner,html,copy,development,production,analyzer}.ts` plus `types.ts` (placed under `modules/` so they do not mix with the real webpack plugins in `src/plugins/`); plugin order unchanged and pinned by two new tests; the default `index.ejs` is resolved through `utils/package-root.ts` shared with the banner. `codestyle/src/rules/{typescript,style,files,react,tests,globs}.ts`; the serialized ESLint config is byte-identical. No `@sonar/cognitive-complexity` disables are left in either file._
- [x] **H5. Dependency cycles and build order.** Remove `@rockpack/codestyle` from the devDependencies of babel, utils and tester by linting them from a root `eslint.config.ts` that imports `packages/codestyle/src` (the root already resolves it through the workspace). Declare `@rockpack/compiler` as a devDependency where a build script imports it (done on 2026-09-23 for codestyle and starter). Then replace the hand-ordered root `build` chain with `lerna run build` (topological) and keep the per-package `build:*` scripts for local use. Acceptance: `lerna run build` prints no cycle warning. _Done 2026-09-24 with a different mechanism than proposed: the devDependencies stay declared (they are real), and each package's `nx.implicitDependencies` excludes `@rockpack/codestyle` and `@rockpack/tester` from its graph, which removes every cycle (they come from lint and unit-test tooling, including tester, which the root eslint config idea would not fix). `nx.json` sets `build.dependsOn: ["^build"]`; root `build` is one topological `lerna run build` over the six packages (order utils, babel, codestyle, tester, compiler, starter). No cycle warning in build, lint or test runs._
- [x] **H6. Dead compiler modules.** `compiler/src/utils/common-multi-validators.ts` (`commonMultiValidator`) and `compiler/src/utils/find-html.ts` (`findHtml`) are neither imported nor exported. Found in Plan 1 Phase 6.4, which covers them with small specs so coverage stays honest. Delete both modules together with their specs and the now-unused `errors/markup-compiler.ts` messages, or wire them in if a markup compiler is planned. _Done 2026-09-24 as part of F2._

## 11. Area I: test-suite recommendations outside `packages/*`

- [x] **I1. `describe` structure** (CLAUDE.md): rewrite these specs into `negative cases` / `positive cases` blocks: `examples/tester/{debug,simple,typescript}/src/sum.spec.ts`, `examples/tester/es2015+/src/index.spec.ts`, `examples/tester/sinon/src/user-utils.spec.ts`, `examples/tester/graphql/src/app.spec.tsx`, `examples/tester/rest/src/app.spec.tsx`, `examples/tester/react/src/{link,my-component}.spec.tsx`, `examples/tester/node-jest-environment/src/index.spec.ts`, `examples/tester/node-jest-extend/src/index.spec.ts`, `e2e/babel-e2e/src/main.spec.ts`, `e2e/starter-e2e/src/*.spec.ts`, and the starter templates under `packages/starter/templates/addons/tester/**` (`index.spec.ts(x)`, `hooks/*.spec.tsx`, `api/*.spec.ts`, `components/tags`, `components/feature-card`). Templates csr and ssr `components/` are byte-identical: keep them identical or share them (Plan 3). _Done 2026-09-24 for the tester examples, `e2e/babel-e2e` and the starter templates (verified in generated csr, ssr, component and library projects); `e2e/starter-e2e` moves to Plan 4 with A6._
- [x] **I2. Determinism in examples and templates:** `examples/tester/es2015+/src/index.spec.ts` (real 1000 ms wait) and the template `api/rockpack.api.spec.ts` (real 600 ms) use `jest.useFakeTimers()`; `examples/tester/react/src/link.spec.tsx` snapshots `asFragment()` instead of the whole RTL render result; remove the `debugger` in `examples/tester/debug/src/sum.spec.ts`; sinon example stubs in `beforeEach` with `restore` in `afterEach`. _Done 2026-09-24 together with I1: fake timers in `es2015+` and the template `rockpack.api.spec.ts`, `asFragment()` snapshot, no `debugger`, sinon stubs per test._
- [x] **I3. `e2e/babel-e2e`.** Decision deferred to [Plan 4, section 6](./04-e2e.md) (packaging e2e). After Plan 1 Phase 2 it duplicates `packages/babel/src/index.spec.ts`. Keep it as a five-line smoke test of the built `lib` (import from `@rockpack/babel`, assert one preset) and delete the rest, or delete the workspace. Also drop the copied `modulePathIgnorePatterns: ['./src/generators/']` in its `scripts.test.ts`. _Done 2026-09-24: kept as a smoke test of the built package; the ESM and CJS entries are loaded in real Node processes (Jest resolves every import through the CJS condition), plus the E11 integration spec. `modulePathIgnorePatterns` removed. Decision recorded in Plan 4 section 6._ _Confirmed in Plan 4 step 9 (2026-09-24): installed tarballs are covered by `e2e/packaging-e2e`; `babel-e2e` stays as the smoke test of the built entries._
- [x] **I4. Tester defaults for consumers** (`packages/tester/src/configs/config-compiler.ts`): make `maxWorkers: 1`, `runInBand` and `noCache` opt-in (`TesterOptions.serial?: boolean`, default `false`), keep `--watch` fast. Add `collectCoverageFrom: ['src/**/*.{ts,tsx,js,jsx}', '!**/*.spec.*', '!**/*.test.*']` as the default so coverage counts untested files. Add `text-summary` and `lcov` reporters (done in Plan 1 Phase 0.2 if not already). _Done 2026-09-24: `serial` option; default runs are parallel with cache (the six package suites went to about 5 s); `e2e/starter-e2e` opts into `serial`. The default `collectCoverageFrom` follows the `src` folders._
- [x] **I5. Repository hygiene.** Delete the stray per-example `package-lock.json` files (`examples/compiler/{css-modules,imagemin,react-app,ts-css-modules}`, `examples/tester/{graphql,react,rest,sinon,typescript}`), add `**/package-lock.json` except the root one to `.gitignore`, add `.DS_Store` cleanup, and remove the empty `NO_COMMIT/` directory or document it in `.gitignore`. _Done 2026-09-24: nine stray lockfiles removed, `**/package-lock.json` ignored except the root one. `.DS_Store` and `NO_COMMIT/` were already ignored; `NO_COMMIT/` is a local folder and was left in place._

## 12. Area J: documentation

- [x] **J1.** README "Contributing": Node 24, `npm i`, `npm run build`, `npm run lint`, `npm run test:unit`, `npm test`; the release flow (B5, B6); how the updater works (`--dry-run`, `--no-major`, peer resolution). _Done 2026-09-24._
- [x] **J2.** Starter README: document `--type`, `--tests`, `--no-install`, `--mode=test`, `--folder`, `--yarn`. _Done 2026-09-24._
- [x] **J3.** Tester README: correct import, `--watch`, `serial` option, coverage configuration and thresholds, `moduleNameMapper` for `.js` imports. _Done 2026-09-24._
- [x] **J4.** CHANGELOG `9.0.0`: one bullet per behavioural change in this plan (F, I4, C2 peer dependency, D1 engines, E2 hooks). _Done 2026-09-24; also corrected the entry that said `rockpack.babel.js` support was dropped (only the SSR template file was removed)._
- [x] **J5.** `roadmap.md`: mark "Fix husky initialization" done (E2), add the Plan 3 items as pending. _Done 2026-09-24._

## 13. Acceptance for the whole plan

1. CI runs on every PR and on `next`; `starter-e2e` runs nightly and passes. _Superseded by Plan 4 section 7: the starter suite runs in `e2e-pinned` on every push and pull request and in `e2e-runtime`; nothing runs nightly for now._
2. `npx knip` and `syncpack lint` are clean; `npm ls` shows one version per shared dependency.
3. `grep -rn "process.exit" packages/compiler/src` matches only `error-handler.ts` and the exported compiler entry points.
4. `grep -rn "interface " packages/*/src` matches only module augmentation blocks.
5. Every spec in the repository follows the negative/positive structure.
6. All Plan 1 thresholds still hold.

## 14. Suggested order

| Step | Items | Size |
|---|---|---|
| 1 | A1, A3, A4 (CI and hooks first, so everything after is checked automatically) | M |
| 2 | B1, B2, B3, B4, B5, B6 | M |
| 3 | C1 to C7, D1 to D3 | M |
| 4 | E1 to E10 | M |
| 5 | G1 to G4, H1, H2 | M |
| 6 | F1 to F5 | L |
| 7 | H3, H4, H5 | L |
| 8 | I1 to I5, A6, J1 to J5 | M |
