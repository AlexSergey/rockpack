{
  "$schema": "./node_modules/syncpack/schema.json",
  "versionGroups": [
    {
      "label": "Peer dependencies declare ranges, not the pinned versions used for development",
      "dependencyTypes": ["peer"],
      "isIgnored": true
    },
    {
      "label": "book prerenders with puppeteer 7; moving it to the current major belongs to the book build rework (Plan 3)",
      "packages": ["@rockpack/book"],
      "dependencies": ["puppeteer"],
      "isIgnored": true
    }
  ]
}
