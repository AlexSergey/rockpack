import { LocalizationObserver } from '@localaser/component';
import ru from './locales/ru.json';

class Root extends Component {
  render() {
    return (
      <LocalizationObserver
        currentLanguage={this.state.currentLanguage}
        languages={{ ru }
      >
        <App/>
      </LocalizationObserver>
    )
  }
}
