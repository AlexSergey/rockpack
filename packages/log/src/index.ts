import LoggerContainer, { useLogger, logger } from './LoggerContainer';
import { LoggerInterface } from './types';

export { LoggerContainer };
export { useLogger };
export { LoggerInterface };

export default logger;
