{
  "scripts": {
    "typing": "tsc --noEmit",
    "lint:production": "eslint -c .eslintrc.production.js 'src/**/*.{js,jsx,ts,tsx}' --quiet"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run typing && npm run lint:production"
    }
  }
}
